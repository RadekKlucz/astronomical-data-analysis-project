asteroids <- read.table("./data/asteroids.txt", header = T)
asteroids <- read.table("./data/asteroids.txt", header = T)
asteroids <- asteroids[order(asteroids$Dens),]
names <- asteroids[, 1]
dens <- asteroids[, 2]
err <- asteroids[, 3]
galaxies1 <- read.table('./data/GlobClus_M31.dat', header = T)
galaxies2 <- read.table('./data/GlobClus_MWG.dat', header = T)
M31 <- galaxies1[,2]  ; MW <- galaxies2[,2]
asteroids <- read.table('https://astrostatistics.psu.edu/MSMA/datasets/GlobClus_MWG.dat', header = T)
asteroids <- asteroids[order(asteroids$Dens),]
asteroids
./data/asteroids.txt
setwd()
setwd("C:/Users/Lenovo/Desktop/Github/R-project/main")
asteroids <- read.table("./data/asteroids.txt", header = T)
asteroids <- asteroids[order(asteroids$Dens),]
names <- asteroids[, 1]
dens <- asteroids[, 2]
err <- asteroids[, 3]
pwd
# Set working directory. If you don't know the path, enter pwd in the terminal to find your path
setwd(/c/Users/Lenovo/Desktop/Github/R-project/main)
pwd()
setwd("C:/Users/Lenovo/Desktop/Github/R-project/main")
asteroids <- read.table("~/data/asteroids.txt", header = T)
asteroids <- read.table("/data/asteroids.txt", header = T)
asteroids <- read.table("./data/asteroids.txt", header = T)
asteroids <- asteroids[order(asteroids$Dens),]
names <- asteroids[, 1]
dens <- asteroids[, 2]
err <- asteroids[, 3]
# Load data with the file
asteroids <- read.table("./data/asteroids.txt", header = T)
asteroids
asteroids <- asteroids[order(asteroids$Dens),]
asteroids
names
dens
asteroids_frame <- as.data.frame(names, dens, err)
asteroids_frame <- as.data.frame(col=names, col=dens, col=err)
names
type(names)
heder(names)
header(names)
head(names)
t.(names)
# Load data with the file from data folder
asteroids <- read.table("./data/asteroids.txt", header = T)
asteroids <- asteroids[order(asteroids$Dens),]
asteroids
# Load data with the file from data folder
asteroids <- read.table("./asteroids.txt", header = T)
asteroids
asteroids <- asteroids[order(asteroids$Dens),]
names <- asteroids[, 1]
dens <- asteroids[, 2]
err <- asteroids[, 3]
names
asteroids_frame <- as.data.frame(names, dens, err)
asteroids
asteroids_frame
asteroids_frame <- as.data.frame(dens, err)
asteroids_frame
# Load data with the file from data folder
asteroids <- read.table("C:/Users/Lenovo/Desktop/Github/R-project/main/asteroids.txt", header = T)
asteroids <- asteroids[order(asteroids$Dens),]
names <- asteroids[, 1]
dens <- asteroids[, 2]
err <- asteroids[, 3]
asteroids_frame
asteroids
# Load data with the file from data folder
asteroids <- read.csv("./data/asteroids.txt", header = T)
asteroids <- asteroids[order(asteroids$Dens),]
View(asteroids)
# Load data with the file from data folder
asteroids <- read.csv("./data/asteroids.txt", header = T, sep = ' ')
# Load data with the file from data folder
asteroids <- read.csv("./data/asteroids.txt", header = T, sep = '\t')
View(asteroids)
# Load data with the file from data folder
asteroids <- read.table("./data/asteroids.txt", header = T)
View(asteroids)
asteroids <- asteroids[order(asteroids$Dens),]
View(asteroids)
# Sort data using second column
asteroids <- asteroids[sort.list(asteroids$Dens)]
# Sort data using second column
asteroids <- asteroids[sort.list(asteroids, Dens)]
# Sort data using second column
asteroids <- asteroids[sort.list(Dens)]
# Sort data using second column
asteroids <- asteroids[sort.list(asterioids)]
# Sort data using second column
asteroids <- asteroids[sort.list(asteroids)]
# Sort data using second column
asteroids <- asteroids[sort.list(asteroids, 2)]
# Sort data using second column
asteroids_sorted <- asteroids[order(asteroids$Dens), ]
names <- asteroids[, 1]
dens <- asteroids[, 2]
err <- asteroids[, 3]
```{r include=TRUE}
summary(asteroids)
# Set working directory. If you don't know the path, enter pwd in the terminal to find your path
setwd("C:/Users/Lenovo/Desktop/Github/R-project/main")
# Load data with the file from data folder
asteroids <- read.table("./data/asteroids.txt", header = T)
# Sort data using second column
asteroids_sorted <- asteroids[order(asteroids$Dens), ]
# Assign columns
names <- asteroids[, 1]
dens <- asteroids[, 2]
err <- asteroids[, 3]
summary(asteroids)
S1 <- sd(dens)
S2 <- sd(err)
SE1 <- (S1 * 1.253) / (sqrt(26)) #median error
SE2 <- (S2 * 1.253) / (sqrt(26))
S1 <- sd(dens)
S2 <- sd(err)
SE1 <- (S1 * 1.253) / (sqrt(26)) #median error
SE2 <- (S2 * 1.253) / (sqrt(26))
S1 <- sd(dens)
S2 <- sd(err)
SE1 <- (S1 * 1.253) / (sqrt(26)) #median error
SE2 <- (S2 * 1.253) / (sqrt(26))
SE1
SE2
standard_deviation_densinity <- sd(densinity)
# Set working directory. If you don't know the path, enter pwd in the terminal to find your path
setwd("C:/Users/Lenovo/Desktop/Github/R-project/main")
# Load data with the file from data folder
asteroids <- read.table("./data/asteroids.txt", header = T)
# Sort data using second column
asteroids_sorted <- asteroids[order(asteroids$Dens), ]
# Assign columns
names <- asteroids[, 1]
densinity <- asteroids[, 2]
error <- asteroids[, 3]
standard_deviation_densinity <- sd(densinity)
standard_deviation_err <- sd(error)
SE_densinity <- (S1 * 1.253) / (sqrt(26)) #median error
SE_error <- (S2 * 1.253) / (sqrt(26))
summary(asteroids)
# Clculate standard deviation
standard_deviation_densinity <- sd(densinity)
standard_deviation_err <- sd(error)
# Calculate median standard error
se_densinity <- (standard_deviation_densinity * 1.253) / (sqrt(26)) #median error
se_error <- (standard_deviation_err * 1.253) / (sqrt(26))
# Clculate standard deviation
standard_deviation_densinity <- sd(densinity)
standard_deviation_err <- sd(error)
# Calculate median standard error
se_densinity <- (standard_deviation_densinity * 1.253) / sqrt(26)
se_error <- (standard_deviation_err * 1.253) / sqrt(26)
se_densinity
se_error
figure_data <- dotchart(dens, labels = names, cex = 0.9, xlab = expression(Gęstość ~ (g / cm ^ 3)))
graph_data <- dotchart(dens, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
```{r figure 1}
graph_data <- dotchart(dens, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
print(graph_data)
print(graph_data)
dotchart(dens, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
```{r}
dotchart(dens, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
dotchart(dens, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
graph_data <- dotchart(densinity, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
graph_data <- dotchart(densinity, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
se_densinity
se_error
dotchart(densinity, labels = names, cex = 0.9, xlab = expression(Gęstość ~ (g / cm ^ 3)))
dotchart(densinity, labels = names, cex = 0.9, xlab = expression(Gęstość ~ (g / cm ^ 3)))
dotchart(densinity, labels = names)
dotchart(densinity, labels = names, cex = 0.9, xlab = expression(Gęstość ~ (g / cm ^ 3)))
# Set working directory. If you don't know the path, enter pwd in the terminal to find your path
setwd("C:/Users/Lenovo/Desktop/Github/R-project/main")
# Load data with the file from data folder
asteroids <- read.table("./data/asteroids.txt", header = T)
# Sort data using second column
asteroids_sorted <- asteroids[order(asteroids$Dens), ]
# Assign columns
names <- asteroids[, 1]
densinity <- asteroids[, 2]
error <- asteroids[, 3]
summary(asteroids)
# Clculate standard deviation
standard_deviation_densinity <- sd(densinity)
standard_deviation_err <- sd(error)
# Calculate median standard error
se_densinity <- (standard_deviation_densinity * 1.253) / sqrt(26)
se_error <- (standard_deviation_err * 1.253) / sqrt(26)
se_densinity
se_error
dotchart(densinity, labels = names, cex = 0.9, xlab = expression(Gęstość ~ (g / cm ^ 3)))
graph_data <- dotchart(densinity, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
graph_errors <- plot(densinity, ylim = c(0, 8), xlab = "Asteroids", ylab = expression(Densinity ~ (g / cm ^ 3)), pch = 20)
graph_errors <- plot(densinity, ylim = c(0, 8), xlab = "Asteroids", ylab = expression(Densinity ~ (g / cm ^ 3)), pch = 20)
num <- seq(1, length(densinity))
segments(num, dens + err, num, dens - err)
num <- seq(1, length(densinity))
segments(num, densinity + err, num, dens - err)
num <- seq(1, length(densinity))
segments(num, densinity + error, num, densinity - error)
# Set working directory. If you don't know the path, enter pwd in the terminal to find your path
setwd("C:/Users/Lenovo/Desktop/Github/R-project/main")
# Load data with the file from data folder
asteroids <- read.table("./data/asteroids.txt", header = T)
# Sort data using second column
asteroids_sorted <- asteroids[order(asteroids$Dens), ]
# Assign columns
names <- asteroids[, 1]
densinity <- asteroids[, 2]
error <- asteroids[, 3]
summary(asteroids)
# Clculate standard deviation
standard_deviation_densinity <- sd(densinity)
standard_deviation_err <- sd(error)
# Calculate median standard error
se_densinity <- (standard_deviation_densinity * 1.253) / sqrt(26)
se_error <- (standard_deviation_err * 1.253) / sqrt(26)
# Clculate standard deviation
standard_deviation_densinity <- sd(densinity)
standard_deviation_err <- sd(error)
# Calculate median standard error
se_densinity <- (standard_deviation_densinity * 1.253) / sqrt(26)
se_error <- (standard_deviation_err * 1.253) / sqrt(26)
se_densinity
se_error
graph_data <- dotchart(densinity, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
print(graph_data)
# Set working directory. If you don't know the path, enter pwd in the terminal to find your path
setwd("C:/Users/Lenovo/Desktop/Github/R-project/main")
# Load data with the file from data folder
asteroids <- read.table("./data/asteroids.txt", header = T)
# Sort data using second column
asteroids_sorted <- asteroids[order(asteroids$Dens), ]
# Assign columns
names <- asteroids_sorted[, 1]
densinity <- asteroids_sorted[, 2]
error <- asteroids_sorted[, 3]
summary(asteroids)
# Clculate standard deviation
standard_deviation_densinity <- sd(densinity)
standard_deviation_err <- sd(error)
# Calculate median standard error
se_densinity <- (standard_deviation_densinity * 1.253) / sqrt(26)
se_error <- (standard_deviation_err * 1.253) / sqrt(26)
se_densinity
se_error
graph_data <- dotchart(densinity, labels = names, cex = 0.9, xlab = expression(Densinity ~ (g / cm ^ 3)))
print(graph_data)
graph_errors <- plot(densinity, ylim = c(0, 8), xlab = "Asteroids", ylab = expression(Densinity ~ (g / cm ^ 3)), pch = 20)
print(graph_errors)
graph_errors <- plot(densinity, ylim = c(0, 8), xlab = "Asteroids", ylab = expression(Densinity ~ (g / cm ^ 3)), pch = 20)
num <- seq(1, length(densinity))
segments(num, densinity + error, num, densinity - error)
print(graph_errors)
graph_errors <- plot(densinity, ylim = c(0, 8), xlab = "Asteroids", ylab = expression(Densinity ~ (g / cm ^ 3)), pch = 20, grid())
num <- seq(1, length(densinity))
segments(num, densinity + error, num, densinity - error)
print(graph_errors)
graph_errors <- plot(densinity, ylim = c(0, 8), xlab = "Asteroids", ylab = expression(Densinity ~ (g / cm ^ 3)), pch = 20)
num <- seq(1, length(densinity))
segments(num, densinity + error, num, densinity - error)
print(grid(graph_errors)
graph_errors <- plot(densinity, ylim = c(0, 8), xlab = "Asteroids", ylab = expression(Densinity ~ (g / cm ^ 3)), pch = 20)
num <- seq(1, length(densinity))
segments(num, densinity + error, num, densinity - error)
print(grid(graph_errors))
graph_errors <- plot(densinity, ylim = c(0, 8), xlab = "Asteroids", ylab = expression(Densinity ~ (g / cm ^ 3)), pch = 20) + grid()
num <- seq(1, length(densinity))
segments(num, densinity + error, num, densinity - error)
print(graph_errors)
graph_comprasion <- boxplot(asteroids[, 2:3], varwidth = T, notch = T, xlab = "Asteroidy", ylab = "Gęstość", pars = list(boxwex = 0.3, boxlwd = 1.5, whisklwd = 1.5, staplelwd = 1.5, outlwd = 1.5, font = 2)) + grid()
print(graph_comprasion)
# Load the required libraries
library(nortest)
library(outliers)
# Set working directory. If you don't know the path, enter pwd in the terminal to find your path
setwd("C:/Users/Lenovo/Desktop/Github/R-project/main")
# Load data with the file from data folder
asteroids <- read.table("./data/asteroids.txt", header = T)
# Sort data using second column
asteroids_sorted <- asteroids[order(asteroids$Dens),]
# Assign columns
names <- asteroids_sorted[, 1]
densinity <- asteroids_sorted[, 2]
error <- asteroids_sorted[, 3]
shapiro.test(densinity)
shapiro.test(error)
dixon.test(dens)
dixon.test(densinity)
galaxies1 <- read.table('./data/GlobClus_M31.dat', header = T)
galaxies2 <- read.table('./data/GlobClus_MWG.dat', header = T)
M31 <- galaxies1[,2]  ; MW <- galaxies2[,2]
summary(galaxies_MW)
# Load globular clusters data from two files
galaxies_M31 <- read.table('./data/GlobClus_M31.dat', header = T)
galaxies_MW <- read.table('./data/GlobClus_MWG.dat', header = T)
# Assing variables
m_31 <- galaxies_M31[,2]  ;
milky_way <- galaxies_MW[,2]
summary(galaxies_MW)
H1 <- hist(milky_way)
H2 <- hist(m_31)
histogram_milky_way <- hist(milky_way)
histogram_m_31 <- hist(m_31)
print(histogram_milky_way)
print(histogram_m_31)
module <- median(milky_way)-median(m_31)
module
module <- median(milky_way)-median(m_31)
print(module)
